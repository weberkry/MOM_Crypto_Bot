[Unit]
Description=Monitoring Stack (InfluxDB, Telegraf, Grafana)
Requires=docker.service
After=docker.service

[Service]
Restart=always
WorkingDirectory=/home/christiane/git/MOM_Crypto_Bot/crypto_risk_analytics/setup
ExecStart=/usr/bin/docker compose up -d
ExecStop=/usr/bin/docker compose down

[Install]
WantedBy=multi-user.target
